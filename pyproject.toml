[project]
name = "vidmagik-mcp"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.0.0",
    "moviepy>=2.2.1",
    "numexpr>=2.14.1",
    "opencv-contrib-python>=4.13.0.92",
    "pytest>=9.0.2",
]

[dependency-groups]
dev = [
    "pytest-cov>=7.0.0",
]
